# Analysis

Run:

```powershell
pip install -r analysis\requirements-analysis.txt
python analysis\run_modeling.py
```

The script trains classification and regression models, selects champions on validation data, evaluates the future test period, saves model pipelines, and produces scoring tables.
